"""
Word Search
Category: Backtracking
Difficulty: medium

Given an m x n grid of characters and a string word, return True if word exists in the grid. The word can be constructed from letters of sequentially adjacent cells (horizontal or vertical). Each cell may only be used once.

Expected Time Complexity: O(m * n * 3^L) where L is word length
Expected Space Complexity: O(L) for recursion stack
"""


def exist(board: list[list[str]], word: str) -> bool:
    # Write your solution here
    pass
