"""
Lowest Common Ancestor of a Binary Tree
Category: Trees
Difficulty: medium

Given a binary tree, find the lowest common ancestor (LCA) of two given nodes.

The tree is provided as a list (level-order), and the two target values are given.
Return the value of the LCA node.

For this problem, a node is represented as [val, left, right] or None. The tree input is a level-order list where None indicates absence.
Example: tree = [3,5,1,6,2,0,8,None,None,7,4], p = 5, q = 1 -> 3

Expected Time Complexity: O(n)
Expected Space Complexity: O(n)
"""


def lowest_common_ancestor(tree: list, p: int, q: int) -> int:
    # Write your solution here
    pass
