"""
Serialize and Deserialize Binary Tree
Category: Trees
Difficulty: hard

Design an algorithm to serialize a binary tree to a string and deserialize that string back to the original tree.

Implement two functions:
  serialize(tree: list) -> str
  deserialize(data: str) -> list

The tree is given as a level-order list. Your serialized format is up to you, but deserialize(serialize(tree)) must return the original tree.

For this assessment, implement a single function that takes a tree (level-order list), serializes it, deserializes it, and returns the reconstructed level-order list.

Expected Time Complexity: O(n)
Expected Space Complexity: O(n)
"""


def serialize_deserialize(tree: list) -> list:
    # Write your solution here
    pass
