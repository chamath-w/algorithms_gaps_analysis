"""
Task Scheduler
Category: Greedy
Difficulty: medium

Given a list of CPU tasks (characters A-Z) and a cooldown period n, return the minimum number of intervals the CPU needs to complete all tasks.
The same task must wait at least n intervals before repeating. The CPU can be idle during cooldown.

Example: tasks = ['A','A','A','B','B','B'], n = 2 -> 8

Expected Time Complexity: O(n) where n is number of tasks
Expected Space Complexity: O(1) - at most 26 letters
"""


def least_interval(tasks: list[str], n: int) -> int:
    # Write your solution here
    pass
