"""
Course Schedule II (Topological Sort)
Category: Graphs
Difficulty: medium

There are numCourses courses labeled 0 to numCourses-1. You are given a list of prerequisites where prerequisites[i] = [ai, bi] means you must take course bi before course ai.

Return an ordering of courses you should take to finish all courses. If impossible, return an empty list.

Example: numCourses = 4, prerequisites = [[1,0],[2,0],[3,1],[3,2]] -> [0,1,2,3] or [0,2,1,3]

Expected Time Complexity: O(V + E)
Expected Space Complexity: O(V + E)
"""


def find_order(numCourses: int, prerequisites: list[list[int]]) -> list[int]:
    # Write your solution here
    pass
