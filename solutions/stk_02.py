"""
Valid Parentheses with Wildcards
Category: Stacks & Queues
Difficulty: medium

Given a string s containing '(', ')' and '*', where '*' can be treated as '(' or ')' or an empty string, determine if s is valid.

A valid string means every '(' has a matching ')' and vice versa, in the correct order.

Expected Time Complexity: O(n)
Expected Space Complexity: O(1)
"""


def check_valid_string(s: str) -> bool:
    # Write your solution here
    pass
