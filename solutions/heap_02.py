"""
Find Median from Data Stream
Category: Heaps & Priority Queues
Difficulty: hard

Design a data structure that supports adding integers and finding the median of all elements added so far.

Implement a class with:
  __init__(self): Initialize.
  add_num(self, num: int): Add an integer.
  find_median(self) -> float: Return the median.

For this assessment, implement a function that takes a list of operations and returns a list of medians after each add_num.
Input: nums (list of integers to add one by one)
Output: list of medians after each insertion

Expected Time Complexity: O(log n) per add, O(1) per find_median
Expected Space Complexity: O(n)
"""


def running_median(nums: list[int]) -> list[float]:
    # Write your solution here
    pass
